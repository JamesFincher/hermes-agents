# Host env for inception. Copy to .env and fill values.
# Never commit .env. Official interpolation: ${env:VAR} in config.yaml / mcp.json.
# https://hermes-agent.nousresearch.com/docs/user-guide/configuration

# MCP (Context7 library docs only)
# CONTEXT7_API_KEY=

# Retrieval fallback (locked gather). Deploy host only.
# SEARXNG_URL=
# FIRECRAWL_API_URL=

# Memory (Honcho Cloud). Self-hosted uses honcho.json baseUrl instead.
# HONCHO_API_KEY=

# Model provider key. This profile does not pin model.default.
# OPENAI_API_KEY=
