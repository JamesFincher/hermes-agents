# finance-captain — copy to the installed profile's .env. Never commit real values.
# Grant READ-ONLY scopes everywhere unless you have deliberately enabled
# mutation_mode: approved_apply and understand what the agent can then change.

QUICKBOOKS_TOKEN=
QUICKBOOKS_MCP_URL=
XERO_TOKEN=
XERO_MCP_URL=
STRIPE_API_KEY=            # restricted key, read-only
PLAID_TOKEN=
PLAID_MCP_URL=
RAMP_TOKEN=
RAMP_MCP_URL=
GSHEETS_MCP_URL=
WAREHOUSE_TOKEN=           # read-only role
WAREHOUSE_MCP_URL=
FRED_MCP_URL=
EDGAR_MCP_URL=

# Deploy-host only: the self-hosted retrieval pair. Not in env_requires.
SEARXNG_URL=http://localhost:8888
FIRECRAWL_API_URL=http://localhost:3002
